# Creating CSS Effects

This project demonstrates custom web typography and interactive CSS image effects.

## Course challenge features

- Google Fonts are imported in the HTML document and applied with `font-family`.
- Image hover effects use `:hover`, `transform`, `filter`, and CSS transitions.
- The layout is responsive for desktop and mobile screens.

